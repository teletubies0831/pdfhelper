# Reading mode module

Owns reading-mode preferences, resolved modes, labels, and strategies.

- Keep strategies pure and independent from viewer DOM.
- Detection requests use the AI public contract.
- Storage compatibility remains in the reading-mode repository or migration layer.
